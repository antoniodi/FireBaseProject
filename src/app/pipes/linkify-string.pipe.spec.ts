import { LinkifyStringPipe } from './linkify-string.pipe';

describe('LinkifyStringPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkifyStringPipe();
    expect(pipe).toBeTruthy();
  });
});
